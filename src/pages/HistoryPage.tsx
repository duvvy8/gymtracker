import { Card, EmptyState, PageHeader } from '../components/ui';

export function HistoryPage() {
  return (
    <>
      <PageHeader title="History" description="This section is not built yet." />
      <Card>
        <EmptyState title="Nothing here yet">
          The History screen is scaffolded. Content arrives in a later build step.
        </EmptyState>
      </Card>
    </>
  );
}
